name: Basir Visual IPA bridge

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: bellinghamfolks-debug/Ion/.github/workflows/build-basir-visual-unsigned.yml@claude/basir-visual-ipa-final
