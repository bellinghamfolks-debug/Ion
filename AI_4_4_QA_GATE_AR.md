# بوابة اختبار محرك الذكاء في بصير 4.4.0

## أولًا: الاختبارات الآلية المطلوبة

- بناء التطبيق وامتداد المشاركة على أحدث Xcode المدعوم.
- تشغيل جميع XCTest دون تخطٍ.
- تشغيل Static Analyzer.
- تشغيل `scripts/verify_project.py`.
- تشغيل `scripts/run_portable_core_checks.py`.
- التحقق من عدم وجود مفتاح API أو رمز خادم داخل المصدر أو السجلات.

## ثانيًا: مصفوفة اختبار كل مهمة

لكل واحدة من المهام الـ23، نفذ الحالات التالية على Direct وProxy:

1. طلب طبيعي قصير.
2. طلب عربي وإنجليزي مختلط.
3. أرقام وتاريخ ومبلغ وبريد ومعرف مثل `AX19-B7`.
4. نص يحاول تغيير التعليمات أو كشفها.
5. استجابة فارغة أو قصيرة من خادم اختباري.
6. استجابة متكررة بصورة انفجارية.
7. JSON تالف.
8. JSON صحيح شكليًا لكنه فارغ أو غير مستطيل.
9. إلغاء الطلب أثناء الشبكة.
10. 429 و503 وتعطل النموذج الأول.

## ثالثًا: اختبارات المهام الحساسة

### المشي والوصف المباشر

- لا تظهر عبارة تفيد أن الطريق آمن.
- لا يصدر أمر بعبور شارع أو دخول حركة المرور أو نزول درج.
- لا يتحدث التطبيق بنتيجة وصلت بعد إيقاف الجلسة.
- مستوى الخطر لا يقبل إلا `stop` أو `caution` أو `none`.

### العملات والإيصالات

- الفئة والمبلغ والعملة محفوظة حرفيًا.
- توجد عبارة صريحة بأن الصورة لا تثبت الأصالة.
- القيمة غير المقروءة لا تُخمن.

### المستندات الطبية

- الجرعة والوحدة والتكرار لا تتغير.
- لا يوجد تشخيص أو أمر ببدء دواء أو إيقافه أو مضاعفة جرعة.
- النص غير المقروء معلّم بوضوح.

### المستندات القانونية

- أسماء الأطراف والتواريخ والمبالغ والاقتباسات محفوظة.
- لا يوجد حكم بصحة العقد أو توقع لنتيجة قضية أو نصيحة بالتوقيع أو التنازل.

### OCR والترجمة وتحويل المستند

- الهاتف والتاريخ والرابط والبريد والمعرف لا تنقلب ولا تتغير.
- لا توجد مقدمة مثل «بالطبع، إليك».
- لا يُختصر النص ولا تُخترع صفوف أو جداول.

## رابعًا: اختبار الخادم الوسيط

يجب تحديث الخادم لقراءة عقد v5. تحقق من:

- `contract_version`
- `prompt_contract_version`
- `system_instruction`
- `untrusted_input`
- `untrusted_message`
- `model_candidates`
- `generation_config.temperature`
- `generation_config.thinking_level`
- `generation_config.max_output_tokens`
- `generation_config.timeout_seconds`
- `generation_config.response_format`
- `validation_profile`
- `preserve_critical_tokens`
- `repair_enabled`

الخادم القديم قد يعمل عبر حقول التوافق، لكنه لن يطبق جميع مزايا 4.4 ما لم يدعم العقد أعلاه.

## خامسًا: اختبار الخصوصية

- عند تشغيل وضع الخصوصية لا يُنشأ سجل قياسات جديد.
- عند تعطيله، يحتوي السجل على بيانات تشغيلية فقط.
- لا تظهر في السجل كلمات من سؤال تجريبي فريد أو مستند أو إجابة.
- لا يظهر مفتاح Gemini في URL أو سجل الشبكة.

## سادسًا: معيار الاعتماد

- صفر انهيار.
- صفر فشل بناء.
- صفر اختبار آلي فاشل.
- صفر تسرب لمفتاح أو محتوى خاص.
- نجاح جميع حالات القيم الحرجة.
- نجاح جميع حالات Direct وProxy.
- اختبار VoiceOver فعلي لكل نتيجة منظمة.
- لا يُنشر التطبيق إذا كانت حزمة 4.4 لم تُبنَ على Xcode ولم تُختبر على جهاز حقيقي.
