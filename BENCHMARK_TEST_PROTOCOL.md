# بروتوكول اختبار PDFToWord بالمعيار العلمي

## المتطلبات

- Mac عليه إصدار Xcode المتوافق مع iOS المستهدف.
- iPhone أو iPad حقيقي.
- مفتاح Gemini صالح محفوظ من شاشة الإعدادات.
- ملف `PDF_to_Word_Scientific_Benchmark_AR_v1.pdf` دون تعديل.
- Microsoft Word أو Pages لفحص النتيجة بصريًا.

## إعداد الاختبار

1. افتح `PDFToWord.xcodeproj`.
2. اختر فريق التوقيع وBundle Identifier خاصًا بك.
3. شغّل `Tools/run_checks.sh` من Terminal.
4. ثبّت التطبيق على الجهاز.
5. اترك إعدادات الجودة الصارمة مفعلة، واختر النموذج نفسه في كل الجولات.
6. احذف أي نتيجة قديمة للمعيار قبل الجولة الأولى.

## التنفيذ

1. حوّل الملف ثلاث مرات دون تعديل الناتج.
2. سمِّ النتائج `Benchmark-Run-1.docx` إلى `Benchmark-Run-3.docx`.
3. لا تغيّر النموذج أو الشبكة عمدًا بين الجولات.
4. سجّل زمن كل جولة، عدد الصفحات الاحتياطية، والتحذيرات.
5. شغّل على كل ملف:

```bash
python3 Tools/evaluate_scientific_benchmark.py Benchmark-Run-1.docx --json Run-1.json
```

## شرط الاجتياز الممتاز

- 95 من 100 أو أكثر في كل جولة.
- لا يوجد قسم حرج أقل من 4 من 5.
- لا يوجد صف مفقود أو مكرر من `ROW-01` إلى `ROW-30`.
- لا يوجد سطر مفقود أو مكرر من `DENSE-001` إلى `DENSE-048`.
- الصفحة منخفضة الجودة محفوظة بصريًا عند عدم كفاية الثقة.
- الصور والمخططات والختم والتوقيع لها أوصاف بديلة غير فارغة.
- فتح الملف في Microsoft Word دون رسالة إصلاح.
- قراءة التسلسل الأساسي عبر VoiceOver أو قارئ شاشة Word دون خلط الرؤوس بالمتن.

## الفحص اليدوي الإلزامي

الأداة الآلية لا تستطيع قياس التطابق البصري الكامل. افحص يدويًا:

- اتجاه النص المختلط في الصفحة 3.
- اللون والتظليل والحجم والشطب في الصفحة 4.
- ترتيب الأعمدة في الصفحة 6.
- مواضع الصور ودقتها في الصفحة 12.
- الرسم البياني والمعادلات في الصفحتين 13 و14.
- المساحة الفارغة والعناصر المدورة في الصفحة 19.

## تفسير الصفحات الاحتياطية

حفظ صفحة كاملة كصورة ليس فشلًا تلقائيًا. في الصفحة المنخفضة الجودة هو السلوك الآمن المطلوب عند عدم إمكان إثبات النص. لكن استخدام الصورة الاحتياطية في صفحة رقمية واضحة يخفض جودة النتيجة ويجب التحقيق فيه.

## بصمة ملف المعيار المستخدم في هذه الدفعة

```text
SHA-256: 8009747e444f90d2a19d352435ce3d1aa9cec86f9f5e2afddc14ef2f72663d7f
```

استخدم البصمة للتأكد من أن ملف الاختبار لم يتغير قبل مقارنة الجولات.
