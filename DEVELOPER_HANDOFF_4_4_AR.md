# تعليمات تسليم بصير iOS 4.4.0 للمطور

## نقطة البداية

استخدم مصدر 4.4.0 الكامل المرفق. لا تطبق التعديلات على نسخة أقدم بصورة انتقائية، لأن محرك السياسات موزع على عدة ملفات مترابطة.

## خطوات البناء

1. ثبّت أحدث Xcode المدعوم وXcodeGen.
2. من مجلد `ios` نفذ `xcodegen generate`.
3. افتح المشروع الناتج.
4. اضبط Development Team ومعرفات الحزمة للتطبيق والامتداد.
5. ابنِ مخطط `Basir` للمحاكي.
6. شغّل اختبارات `BasirTests`.
7. شغّل Static Analyzer.
8. اختبر Direct بمفتاح تجريبي محدود الصلاحية.
9. اختبر Proxy بخادم يدعم عقد `basir-ai-2026.06-v5`.
10. نفذ قائمة `AI_4_4_QA_GATE_AR.md` على iPhone فعلي مع VoiceOver.

## متطلبات الخادم الوسيط

الخادم مسؤول عن:

- اختيار أول نموذج متاح من `model_candidates`.
- تطبيق إعدادات `generation_config`.
- وضع المفتاح في الترويسة، لا في URL.
- احترام المهلة والإلغاء قدر الإمكان.
- إعادة `answer` أو `text`.
- إعادة `model_version` و`usage_metadata` عند توفرهما.
- عدم دمج `untrusted_input` في تعليمات النظام.
- عدم تسجيل الصور أو المستندات أو الأوامر أو الإجابات افتراضيًا.

## نقاط لا يجوز حذفها أثناء الدمج

- `AITaskPolicyCatalog` ومهامه الـ23.
- حدود البيانات العشوائية.
- `responseFormat` الحالي للمخرجات المنظمة.
- التحقق المحلي حتى عند استخدام JSON Schema.
- فحص القيم الحرجة.
- محاولة الإصلاح الآمنة.
- نموذج بديل بعد فشل النموذج الأول.
- تعطيل القياسات في وضع الخصوصية.
- عدم تضمين المرشح المرفوض في أمر الإصلاح.

## ترقية الخادم القديم

حقلا `input` و`instruction` موجودان للتوافق المؤقت. لا تعتمد عليهما في تطوير جديد. انتقل إلى:

- `system_instruction`
- `untrusted_message`
- `untrusted_input`
- `generation_config`

بعد ترقية جميع الخوادم والعملاء يمكن حذف حقول التوافق في إصدار رئيسي لاحق بعد فترة انتقال معلنة.

## ملاحظات التشغيل

- `gemini-3.1-pro-preview` نموذج Preview؛ يجب مراقبة توفره، لكن التطبيق يملك بدائل تلقائية.
- لا تغير أسماء النماذج دون تحديث الاختبارات والتقرير.
- لا ترفع حدود الصور أو النصوص أو مخططات JSON دون اختبار ذاكرة وجهاز فعلي.
- لا تسمح لأي شاشة بإضافة سؤال المستخدم أو نص المستند إلى `systemPrompt`.

## شرط التسليم النهائي

لا تُسلَّم IPA للنشر إلا بعد نجاح:

- Build وArchive.
- XCTest.
- الاختبار على جهاز فعلي.
- Direct وProxy.
- الكاميرا والصوت والموقع والمشاركة.
- VoiceOver وأحجام النص الكبيرة.
- ملف PDF المعياري لتحويل Word.
