# خطة اختبار الإصدار 2.0.0 قبل الاعتماد اليومي

لا يوصف التطبيق بأنه معتمد نهائيًا أو محققًا لدقة 95% قبل تنفيذ هذه الخطة و`ACCURACY_PROTOCOL.md` على Mac وiPhone حقيقي، ثم فتح النتائج في Microsoft Word وفحصها بقارئ الشاشة.

## 1. البناء

- تشغيل `bash Tools/run_checks.sh` والتأكد من نجاح كل مرحلة.
- Build بنمط Debug على iPhone Simulator.
- Build وتشغيل على iPhone حقيقي بنظام iOS 17 أو أحدث.
- Archive بنمط Release، أولًا دون توقيع ثم بالتوقيع الحقيقي.
- تفعيل Treat Warnings as Errors ومراجعة تحذيرات Swift Concurrency.
- التأكد من عدم وجود تحذيرات Privacy Manifest أو App Icon أو PDFKit أو Vision.

## 2. المفتاح والنموذج

- مفتاح صحيح مع `gemini-3.5-flash`.
- مفتاح خاطئ ومفتاح محظور ومفتاح بلا حصة.
- نموذج مخصص صحيح ثم نموذج غير موجود.
- بدء التحويل والتأكد من ظهور اختبار Structured Output.
- التحقق من استخدام ثلاث قراءات للصفحة غير الفارغة.
- قطع الشبكة بعد اكتمال الصفحات ثم استئناف بناء Word دون إعادة إرسالها.

## 3. مصفوفة PDF الأساسية

استخدم 30 ملفًا على الأقل تشمل العربية والإنجليزية المختلطة، ومستندات قانونية، ومسحًا ضوئيًا باهتًا ومائلًا، وصفحات فارغة وصورة فقط، وجداول متعددة الصفحات، وأعمدة، ورسومًا بيانية، وأختامًا وتواقيع، وصفحات طولية وعرضية، وملفات 50 و200 صفحة، وملفات تالفة ومشفرة وخطوطًا غير قياسية.

أضف إلى ذلك 20 ملف خط يد عربي و20 ملف خط يد إنجليزي على الأقل للاختبار الوظيفي، ثم استخدم المجموعة الأكبر المحددة في بروتوكول الدقة للاعتماد الرقمي.

## 4. فحوص الدقة

لكل ملف:

- مقارنة عدد الصفحات في PDF وWord.
- التأكد من عدم سقوط صفحة أو تكرارها.
- مقارنة العناوين والأرقام والأسماء والتواريخ والقيم المالية حرفيًا.
- مقارنة الجداول صفًا وصفًا وخلية بخلية.
- التأكد من عدم ظهور فقرة غير موجودة في الأصل.
- التأكد من تصنيف صفحات خط اليد والمختلطة وإظهارها في السجل.
- التأكد من أن أقل درجة داخلية للمهمة لا تقل عن 95%.
- رفض الملف إذا كان فيه خطأ غير معلن للمستخدم.

## 5. اختبار بوابة 95%

- حقن نتيجتين مختلفتين عمدًا والتأكد من رفض الصفحة.
- نتيجة ذات اتفاق 94.9% يجب ألا تمر.
- نتيجة ذات اتفاق 95% وجودة مرجعية أقل من 95% يجب ألا تمر.
- صفحة خط يد منخفضة الثقة يجب أن تتوقف ولا تتحول إلى تخمين سياقي.
- صفحة لا تحتوي طبقة نص ولا OCR موثوقًا يجب أن تعتمد على اتفاق القراءات الثلاث، لا على نص مختلق.
- مهمة قديمة من الإصدار 1.2 يجب أن تعيد تحليل الصفحات بدل استخدام نتائج أحادية القراءة.

## 6. الاستئناف والفشل

- فصل الإنترنت أثناء دفعة متزامنة والتأكد من حفظ كل صفحة نجحت.
- الضغط على الإلغاء أثناء انتهاء الدفعة.
- إغلاق التطبيق بالقوة ثم الاستئناف.
- إعادة تشغيل الجهاز أثناء مهمة طويلة.
- تغيير إعدادات الواجهة ثم استئناف مهمة قديمة والتأكد من استخدام Snapshot.
- إتلاف ملفات المهمة والمصدر وملء مساحة التخزين والتأكد من الفشل الواضح.

## 7. DOCX

- فتح كل ملف في Word على iPhone وMac وWindows إن أمكن.
- عدم ظهور رسالة محتوى غير قابل للقراءة أو طلب إصلاح.
- تشغيل `python3 Tools/validate_docx.py` على كل ناتج.
- التحقق من المقاسات والاتجاهات وفواصل الأقسام والجداول والصور.
- التأكد من أن قصاصات الصور ليست سوداء أو فارغة.
- التأكد من وجود Alt Text مفيد لكل عنصر بصري غير زخرفي.

## 8. VoiceOver وإتاحة الوصول

- التنقل الكامل دون الاعتماد على الرؤية.
- نطق اسم الملف وعدد الصفحات وحالة المفتاح والتقدم.
- نطق أقل درجة جودة وصفحات خط اليد وأسباب الرفض.
- الوصول إلى الصور داخل Word وسماع النص البديل.
- اختبار أكبر أحجام Dynamic Type وأسماء الملفات الطويلة.

## 9. الأداء والتكلفة

- مراقبة Memory Graph أثناء 50 و200 صفحة.
- قياس زمن الصفحة واستهلاك الشبكة وعدد طلبات Gemini.
- التأكد من عدم تراكم الصور المؤقتة أو ملفات البناء.
- التأكد من عدم تسرب المفتاح أو مقتطفات المستند في Console.
- تسجيل أن الصفحة غير الفارغة قد تستهلك ثلاثة طلبات، وعدم إخفاء التكلفة عن المستخدم.

## 10. شروط القبول

- نجاح Release Build وArchive.
- صفر صفحات ساقطة أو مكررة دون تحذير.
- صفر ملفات Word تطلب إصلاحًا.
- نجاح الاستئناف بعد الإغلاق القسري وانقطاع الشبكة.
- اجتياز كل فئة في `ACCURACY_PROTOCOL.md` بنسبة 95% أو أكثر بصورة مستقلة.
- عدم وصف التطبيق بأنه يضمن 95% قبل وجود تقرير اختبار قابل لإعادة التنفيذ.

## Arabic and English localization acceptance

1. Set the app language to Arabic in iOS Settings and relaunch it.
2. Verify that all tabs, forms, alerts, progress messages, validation errors, VoiceOver labels, and the app display name are Arabic.
3. Verify that the interface direction is right-to-left and that file names and model identifiers remain readable.
4. Set the app language to English and relaunch it.
5. Verify that the same interface areas are English and the layout is left-to-right.
6. Create and resume a conversion in each language, including a failed-key test and a failed-page test.
7. Confirm that switching the interface language does not change the PDF content, the selected model, the saved API key, or conversion settings.
8. Confirm that an older saved value of `مخصص` migrates to the language-independent custom-model identifier.
