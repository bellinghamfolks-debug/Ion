# خطة اعتماد Basir iOS 3.0

## بوابات آلية

- نجاح `python3 BasirConvertiOS/tools/verify_project.py BasirConvertiOS --source-only`.
- نجاح إنشاء مشروع Xcode من `project.yml` ثم التحقق الكامل.
- نجاح جميع اختبارات XCTest، ومنها عقد الخادم وتطبيع أرقام الصفحات العربية والفارسية.
- نجاح Release build لجهاز iPhone مع Share Extension.
- سلامة ZIP وSHA-256 للـIPA وعدم وجود أي `_CodeSignature` داخله.

## اختبارات تكامل لازمة

- رفع PDF صغير وكبير، واستئناف المهمة بعد قطع الشبكة وإغلاق التطبيق.
- إدخال صفحات مثل `١–٣، ۵؛۷` والتأكد من معالجة الصفحات نفسها فقط.
- تحويل جداول عربية وإنجليزية ومختلطة، مع مقارنة كل خلية بالمصدر.
- اختبار ملفات ممسوحة وخط يد وعناصر صغيرة مثل الأرقام والدرجات وعلامات الصح.
- رفض أي نتيجة ذات صفحة ساقطة، رقم مضاف، معرّف مفقود، أو صورة بلا نص بديل.
- فتح الناتج في Word مع VoiceOver والتحقق من ترتيب القراءة والجداول والنص البديل.

## شرط الاعتماد

لا يُعتمد الإصدار إذا فشل البناء أو الاختبارات، أو إذا قبل التطبيق نتيجة لا تحمل
بيان جودة ناجحًا وتطابقًا في SHA-256 وعدّ الصفحات.

