--- a/BasirConvert/Views/Components.swift
+++ b/BasirConvert/Views/Components.swift
@@ -1,20 +1,58 @@
 import SwiftUI
 
 enum BasirPalette {
-    static let cyan = Color(red: 0.32, green: 0.88, blue: 1.00)
-    static let cyanDeep = Color(red: 0.00, green: 0.36, blue: 0.48)
-    static let indigo = Color(red: 0.03, green: 0.03, blue: 0.04)
-    static let violet = Color(red: 0.18, green: 0.18, blue: 0.22)
+    static let cyan = Color(red: 0.10, green: 0.58, blue: 1.00)
+    static let cyanDeep = Color(red: 0.03, green: 0.28, blue: 0.66)
+    static let indigo = Color(red: 0.04, green: 0.18, blue: 0.43)
+    static let violet = Color(red: 0.10, green: 0.27, blue: 0.60)
     static let primaryText = Color.white
     static let secondaryText = Color.white.opacity(0.76)
     static let tertiaryText = Color.white.opacity(0.58)
 }
 
 struct AuroraBackground: View {
-    var body: some View {
-        Color.black
+    @Environment(\.accessibilityReduceTransparency) private var reduceTransparency
+
+    var body: some View {
+        GeometryReader { geometry in
+            ZStack {
+                LinearGradient(
+                    colors: [
+                        .black,
+                        Color(red: 0.01, green: 0.04, blue: 0.10),
+                        Color(red: 0.01, green: 0.08, blue: 0.19),
+                        .black
+                    ],
+                    startPoint: .topLeading,
+                    endPoint: .bottomTrailing
+                )
+
+                if !reduceTransparency {
+                    Circle()
+                        .fill(BasirPalette.cyan.opacity(0.12))
+                        .frame(width: geometry.size.width * 0.72)
+                        .blur(radius: 56)
+                        .offset(x: -geometry.size.width * 0.34,
+                                y: -geometry.size.height * 0.25)
+                    Circle()
+                        .fill(BasirPalette.violet.opacity(0.16))
+                        .frame(width: geometry.size.width * 0.82)
+                        .blur(radius: 64)
+                        .offset(x: geometry.size.width * 0.40,
+                                y: geometry.size.height * 0.02)
+                    RoundedRectangle(cornerRadius: 90)
+                        .fill(Color.blue.opacity(0.10))
+                        .frame(width: geometry.size.width * 0.92,
+                               height: geometry.size.height * 0.22)
+                        .blur(radius: 46)
+                        .rotationEffect(.degrees(-18))
+                        .offset(x: -geometry.size.width * 0.18,
+                                y: geometry.size.height * 0.36)
+                }
+            }
             .ignoresSafeArea()
-            .accessibilityHidden(true)
+        }
+        .accessibilityHidden(true)
     }
 }
 
@@ -32,8 +70,8 @@
                 ZStack {
                     RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                         .fill(reduceTransparency
-                              ? Color(red: 0.055, green: 0.055, blue: 0.065)
-                              : Color(red: 0.045, green: 0.045, blue: 0.055).opacity(0.96))
+                              ? Color(red: 0.035, green: 0.06, blue: 0.12)
+                              : Color(red: 0.02, green: 0.06, blue: 0.12).opacity(0.90))
                     if !reduceTransparency {
                         RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                             .fill(.thinMaterial)
@@ -130,8 +168,12 @@
         .padding(.vertical, 20)
         .padding(.horizontal, 18)
         .background(
-            Color(red: 0.055, green: 0.055, blue: 0.065),
-            in: RoundedRectangle(cornerRadius: 24, style: .continuous)
+            LinearGradient(
+                colors: [Color(red: 0.03, green: 0.19, blue: 0.44), Color(red: 0.02, green: 0.34, blue: 0.76)],
+                startPoint: .topLeading,
+                endPoint: .bottomTrailing
+            ),
+            in: RoundedRectangle(cornerRadius: 28, style: .continuous)
         )
         .overlay { RoundedRectangle(cornerRadius: 28).stroke(Color.white.opacity(0.18), lineWidth: 1) }
     }
@@ -294,8 +336,7 @@
                 .font(.subheadline.weight(.semibold))
                 .foregroundStyle(BasirPalette.primaryText)
                 .frame(maxWidth: .infinity)
-                .frame(minHeight: 44)
-                .padding(.vertical, 8)
+                .padding(.vertical, 12)
         }
         .buttonStyle(.plain)
         .background(Color.white.opacity(0.10), in: RoundedRectangle(cornerRadius: 16))
--- a/BasirConvert/Views/TaskComposerView.swift
+++ b/BasirConvert/Views/TaskComposerView.swift
@@ -24,7 +24,6 @@
     @State private var customOutputName = ""
     @State private var passwordURL: URL?
     @State private var pdfPassword = ""
-    @State private var showAdvancedSettings = false
 
     private var isTranslation: Bool { operation == .translate }
     private var supportedExtensions: Set<String> {
@@ -64,55 +63,58 @@
                 VStack(alignment: .leading, spacing: 14) {
                     BasirHeroCard(
                         title: isTranslation
-                            ? l10n.t("ترجمة", "Translate")
-                            : l10n.t("تحويل", "Convert"),
+                            ? l10n.t("ترجمة المستند", "Translate a document")
+                            : l10n.t("تحويل إلى Word", "Convert to Word"),
+                        subtitle: isTranslation
+                            ? l10n.t("ترجم مستنداتك مع الحفاظ على النصوص والصور والجداول والروابط.",
+                                     "Clear translation that preserves structure, images, and links.")
+                            : l10n.t("أنشئ ملف Word منظمًا وسهل القراءة، مع الصور والجداول ووصفها.",
+                                     "An organized, readable Word file with images, tables, and descriptions."),
                         systemImage: isTranslation ? "character.book.closed.fill" : "doc.richtext.fill"
                     )
 
+                    if isTranslation { languageCard }
+                    resultStyleCard
+                    sourceOptionsCard
+
                     if pendingURLs.isEmpty {
-                        PrimaryActionButton(
-                            title: l10n.t("اختيار ملف أو صورة", "Choose a file or image"),
-                            systemImage: "plus.circle.fill"
-                        ) { showSourceMenu = true }
+                        InfoCard(
+                            title: l10n.t("اختر مصدر الملف", "Choose a source"),
+                            text: l10n.t(
+                                "اختر من تطبيق الملفات أو مكتبة الصور أو الكاميرا أو ماسح المستندات أو الحافظة.",
+                                "Choose from Files, Photos, Camera, the document scanner, or the clipboard."
+                            ),
+                            systemImage: "square.and.arrow.down.on.square"
+                        )
                     } else {
                         selectedFilesSection
                     }
 
-                    if isTranslation { languageCard }
-                    resultStyleCard
-
-                    DisclosureGroup(isExpanded: $showAdvancedSettings) {
-                        VStack(alignment: .leading, spacing: 14) {
-                            sourceOptionsCard
-                            VStack(alignment: .leading, spacing: 10) {
-                                GlassSectionTitle(title: l10n.t("اسم النتيجة", "Result name"), systemImage: "pencil")
-                                TextField(l10n.t("اختياري", "Optional"), text: $customOutputName)
-                                    .textInputAutocapitalization(.sentences)
-                                    .padding(14)
-                                    .background(Color.white.opacity(0.08), in: RoundedRectangle(cornerRadius: 14))
-                            }
-                            .glassSurface()
-                        }
-                        .padding(.top, 10)
-                    } label: {
-                        Label(l10n.t("إعدادات متقدمة", "Advanced settings"), systemImage: "slider.horizontal.3")
-                            .font(.headline)
-                            .frame(minHeight: 44)
+                    if let pickerError { InlineMessage(text: pickerError, isError: true) }
+
+                    VStack(alignment: .leading, spacing: 10) {
+                        GlassSectionTitle(title: l10n.t("اسم الملف الناتج (اختياري)", "Result name (optional)"),
+                                          systemImage: "pencil")
+                        TextField(l10n.t("مثال: تقرير الاجتماع", "Example: Meeting report"),
+                                  text: $customOutputName)
+                            .textInputAutocapitalization(.sentences)
+                            .padding(14)
+                            .background(Color.white.opacity(0.08), in: RoundedRectangle(cornerRadius: 14))
                     }
-                    .tint(BasirPalette.cyan)
                     .glassSurface()
 
-                    if let pickerError { InlineMessage(text: pickerError, isError: true) }
-
-                    if !pendingURLs.isEmpty {
-                        PrimaryActionButton(
-                            title: isTranslation ? l10n.t("بدء الترجمة", "Start translation")
-                                                 : l10n.t("بدء التحويل", "Start conversion"),
-                            systemImage: "play.fill"
-                        ) {
-                            if !settings.isConfigured { showConfigurationRequired = true }
-                            else { showPrivacyConfirmation = true }
-                        }
+                    PrimaryActionButton(
+                        title: pendingURLs.isEmpty
+                            ? l10n.t("اختيار ملف أو صورة", "Choose a file or image")
+                            : (isTranslation
+                               ? l10n.t("بدء ترجمة العناصر المختارة", "Translate selected items")
+                               : l10n.t("بدء تحويل العناصر المختارة", "Convert selected items")),
+                        systemImage: pendingURLs.isEmpty ? "plus.circle.fill" : "play.fill"
+                    ) {
+                        if pendingURLs.isEmpty { showSourceMenu = true }
+                        else if !settings.isConfigured { showConfigurationRequired = true }
+                        else if !network.snapshot.isConnected { showPrivacyConfirmation = true }
+                        else { showPrivacyConfirmation = true }
                     }
                 }
                 .appScreenContent(bottomPadding: 28)
@@ -235,6 +237,7 @@
                 ForEach(OutputMode.allCases) { Text($0.title(l10n)).tag($0) }
             }
             .pickerStyle(.menu).tint(BasirPalette.cyan)
+            Text(settings.outputMode.detail(l10n)).font(.footnote).foregroundStyle(BasirPalette.secondaryText)
         }
         .glassSurface()
     }
@@ -391,7 +394,6 @@
         customOutputName = ""
         settings.pageSelection = ""
         settings.save()
-        showAdvancedSettings = false
         viewModel.start(pickerURLs: selected, options: options,
                         configuration: settings.configuration, l10n: l10n)
     }
--- a/BasirConvert/Views/SettingsView.swift
+++ b/BasirConvert/Views/SettingsView.swift
@@ -20,6 +20,7 @@
                             systemImage: "gearshape.fill"
                         )
                         languageCard
+                        serverCard
                         networkCard
                         documentCard
                         feedbackCard
@@ -82,6 +83,38 @@
         .glassSurface()
     }
 
+    private var serverCard: some View {
+        VStack(alignment: .leading, spacing: 12) {
+            GlassSectionTitle(title: l10n.t("خادم بصير", "Basir server"), systemImage: "server.rack")
+            Label(
+                settings.isConfigured
+                    ? l10n.t("الاتصال مضمّن في هذه النسخة", "Connection is built into this app")
+                    : l10n.t("لم يُربط الخادم بهذه النسخة بعد", "This build is not connected yet"),
+                systemImage: settings.isConfigured ? "checkmark.shield.fill" : "exclamationmark.triangle.fill"
+            )
+            .foregroundStyle(settings.isConfigured ? .green : .orange)
+            Text(l10n.t(
+                "لا تحتاج إلى إدخال رابط أو رمز دخول.",
+                "No server address or access code is required."
+            ))
+            .font(.footnote)
+            .foregroundStyle(BasirPalette.secondaryText)
+            Button { testServerConnection() } label: {
+                Group {
+                    if testing {
+                        HStack { ProgressView(); Text(l10n.t("جارٍ فحص الاتصال", "Checking connection")) }
+                    } else {
+                        Label(l10n.t("فحص اتصال الخادم", "Check server connection"), systemImage: "network")
+                    }
+                }
+                .frame(maxWidth: .infinity)
+            }
+            .buttonStyle(.borderedProminent)
+            .tint(BasirPalette.cyan)
+            .disabled(testing || !settings.isConfigured)
+        }
+        .glassSurface()
+    }
 
     private var networkCard: some View {
         VStack(alignment: .leading, spacing: 14) {
@@ -102,40 +135,38 @@
     }
 
     private var documentCard: some View {
-        DisclosureGroup {
-            VStack(alignment: .leading, spacing: 14) {
-                Picker(l10n.t("دقة صفحات PDF", "PDF page quality"), selection: $settings.pdfQuality) {
-                    Text(l10n.t("سريعة", "Fast")).tag(PDFQuality.fast)
-                    Text(l10n.t("متوازنة", "Balanced")).tag(PDFQuality.balanced)
-                    Text(l10n.t("عالية", "High")).tag(PDFQuality.accurate)
-                }
-                .pickerStyle(.menu)
-                Toggle(l10n.t("تخطي الصفحات الفارغة", "Skip blank pages"), isOn: $settings.skipBlankPages)
-                Toggle(l10n.t("استخدام نص PDF الأصلي", "Use embedded PDF text"), isOn: $settings.preferPDFText)
-                Toggle(l10n.t("إدراج ملاحظات الشرائح", "Include slide notes"), isOn: $settings.includeSpeakerNotes)
-                Toggle(l10n.t("إدراج الشرائح المخفية", "Include hidden slides"), isOn: $settings.includeHiddenSlides)
-                Toggle(l10n.t("الحفاظ على الروابط", "Preserve links"), isOn: $settings.preserveLinks)
-                Picker(l10n.t("اتجاه صفحات PDF", "PDF rotation"), selection: $settings.rotationCorrection) {
-                    Text(l10n.t("تلقائي", "Automatic")).tag(0)
-                    Text("90°").tag(90)
-                    Text("180°").tag(180)
-                    Text("270°").tag(270)
-                }
-                .pickerStyle(.menu)
-            }
-            .padding(.top, 10)
-        } label: {
-            Label(l10n.t("إعدادات متقدمة", "Advanced settings"), systemImage: "slider.horizontal.3")
-                .font(.headline)
-                .frame(minHeight: 44)
+        VStack(alignment: .leading, spacing: 14) {
+            GlassSectionTitle(title: l10n.t("معالجة المستندات", "Document processing"),
+                              systemImage: "doc.text.magnifyingglass")
+            Picker(l10n.t("دقة صفحات PDF", "PDF page quality"), selection: $settings.pdfQuality) {
+                Text(l10n.t("سريعة", "Fast")).tag(PDFQuality.fast)
+                Text(l10n.t("متوازنة", "Balanced")).tag(PDFQuality.balanced)
+                Text(l10n.t("عالية", "High")).tag(PDFQuality.accurate)
+            }
+            .pickerStyle(.menu)
+            .tint(BasirPalette.cyan)
+            Toggle(l10n.t("تخطي الصفحات الفارغة", "Skip blank pages"), isOn: $settings.skipBlankPages)
+            Toggle(l10n.t("استخدام نص PDF الأصلي عند توفره", "Use embedded PDF text when available"),
+                   isOn: $settings.preferPDFText)
+            Toggle(l10n.t("إدراج ملاحظات الشرائح", "Include slide notes"), isOn: $settings.includeSpeakerNotes)
+            Toggle(l10n.t("إدراج الشرائح المخفية", "Include hidden slides"), isOn: $settings.includeHiddenSlides)
+            Toggle(l10n.t("الحفاظ على الروابط", "Preserve links"), isOn: $settings.preserveLinks)
+            Stepper(value: $settings.concurrentPages, in: 1...3) {
+                Text(l10n.t("صفحات المعالجة المتزامنة: \(settings.concurrentPages)",
+                            "Pages processed together: \(settings.concurrentPages)"))
+            }
+            Picker(l10n.t("تصحيح اتجاه صفحات PDF", "Correct PDF rotation"),
+                   selection: $settings.rotationCorrection) {
+                Text(l10n.t("من دون تغيير", "No change")).tag(0)
+                Text("90°").tag(90)
+                Text("180°").tag(180)
+                Text("270°").tag(270)
+            }
+            .pickerStyle(.menu)
+            .tint(BasirPalette.cyan)
         }
         .tint(BasirPalette.cyan)
         .glassSurface()
-        .onChange(of: settings.skipBlankPages) { _ in OperationFeedback.selectionChanged() }
-        .onChange(of: settings.preferPDFText) { _ in OperationFeedback.selectionChanged() }
-        .onChange(of: settings.includeSpeakerNotes) { _ in OperationFeedback.selectionChanged() }
-        .onChange(of: settings.includeHiddenSlides) { _ in OperationFeedback.selectionChanged() }
-        .onChange(of: settings.preserveLinks) { _ in OperationFeedback.selectionChanged() }
     }
 
     private var feedbackCard: some View {
--- a/BasirConvert/Views/LibraryAndQueueViews.swift
+++ b/BasirConvert/Views/LibraryAndQueueViews.swift
@@ -186,8 +186,7 @@
                             .accessibilityAction(named: l10n.t("حذف المهمة", "Delete task")) {
                                 if job.status != .running { pendingDeleteJobID = job.id }
                             }
-                            .listRowBackground(Color.black)
-                            .listRowSeparator(.hidden)
+                            .listRowBackground(Color(red: 0.02, green: 0.06, blue: 0.12))
                             .swipeActions(edge: .trailing, allowsFullSwipe: false) {
                                 if job.status == .running {
                                     Button { viewModel.pause() } label: { Label(l10n.t("إيقاف مؤقت", "Pause"), systemImage: "pause") }
