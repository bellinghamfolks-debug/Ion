{
  "name": "englishnova-server",
  "version": "1.0.0",
  "description": "EnglishNova backend: email/password and Google authentication, progress sync, AI and content services on PostgreSQL.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/auth.js && node --check src/config.js && node --check src/routes/authRoutes.js && node --check src/routes/progressRoutes.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "docx": "^9.7.1",
    "express": "^4.19.2",
    "jose": "^5.6.3",
    "jsonwebtoken": "^9.0.2",
    "pdf-lib": "^1.17.1",
    "pg": "^8.12.0",
    "unpdf": "^1.7.0"
  }
}
